import React from "react";

export const ReviewItem: React.FC<any> = (props) => {
  return (
    <div className="p-2 border-b text-sm">
      <p>Elemento de Comanda (Extraído)</p>
    </div>
  );
};

export default ReviewItem;
